"""Knowledge-graph construction pipeline for LC-MS/MS metabolomics datasets.

Subpackages:

- ``enpkg.monolith`` — the pipeline itself: data model, enhancers, loaders, RDF
  serialization, and the front-end-agnostic runner.
- ``enpkg.cli`` — the ``enpkg`` command line.
- ``enpkg.scripts`` — one-off database import utilities.
"""
